#!/usr/bin/env bash
(
    export PATH="/conteco/bin/${CONTECO_TYPE}/${CONTECO_NAME}:/conteco/bin/image/executionplane:$CONTECO_EXECUTIONPLANE_ORIGINALPATH"
    . executionplane-invoke "bin/image/.invoke $@"
    . .invoke "$@"
    . executionplane-complete
)
