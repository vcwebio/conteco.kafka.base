#!/usr/bin/env bash

# changing from multiple methods to flag driven behaviour
# arguments need defining and extracting
(
    flags=$1
    rootPath=$2
    . invoke-executionplane "pwd/$rootPath/release $flags"
#    case $method in
#        release|deep-release)
#            .container push
#            ;;
#        load|deep-load)
#            .container pull
#            ;;
#        unload|deep-unload)
#            .container rmi
#            ;;
#    esac
    . complete-executionplane
)
